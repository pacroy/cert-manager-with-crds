apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cert-manager-with-crds.fullname" . }}-test-connection"
  labels:
    {{- include "cert-manager-with-crds.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cert-manager-with-crds.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
